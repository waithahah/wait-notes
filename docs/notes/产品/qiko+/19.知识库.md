# 上传文件
## 接口
```python
/console/api/files/upload?source=datasets
```
## 新增一条分段设置规则
数据表 DatasetProcessRule
# 文本分段与清洗
这里的高质量模式会使用文本生成模型
## 接口
```python
/console/api/datasets/indexing-estimate

inputs:
{
    "info_list": {
        "data_source_type": "upload_file",
        "file_info_list": {
            "file_ids": [
                "87232953-2b0d-4c4e-8b99-b6096196abb8"
            ]
        }
    },
    "indexing_technique": "high_quality",
    "process_rule": {
        "rules": {},
        "mode": "automatic"
    },
    "doc_form": "text_model",
    "doc_language": "Chinese Simplified"
}

outputs:
{
    "total_segments": 1,
    "tokens": 186,
    "total_price": "0.0",
    "currency": "USD",
    "preview": [分段信息]
}

```

# 索引方式
qa索引会用到文本生成模型


# 保存并处理
批次号生成规则：
```python
batch = time.strftime('%Y%m%d%H%M%S') + str(random.randint(100000, 999999))
```


# 触发异步任务执行
```python
    # trigger async task
    if document_ids:
        document_indexing_task.delay(dataset.id, document_ids)
    # 重新索引的时候执行
    if duplicate_document_ids:
        duplicate_document_indexing_task.delay(dataset.id, duplicate_document_ids)
```
## 先把文档更新文索引中状态
然后insert index and update document/segment status to completed