修改host，添加 bl.300188.cn 记录
https://bl.300188.cn/ 
10.100.131.2
【厦门超算】10.100.131.2 mybk/root        Szgt@2025
预发环境：
1. cd /data/apps/NexOmniWeb_Pre/apps/nex-omni-web
2. git pull
3. pnpm run build-only:prod-pre
4. pnpm run docker-build:prod-pre
4.1 输入 1，确认打包
4.2 直接回车，使用默认docker包名

官网更新 替换3步骤 pnpm run build:prod-pre

正式环境：
1. cd /data/apps/NexOmniWeb/apps/nex-omni-web
2. git pull
3. pnpm run build-only:prod
4. pnpm run docker-build:prod
4.1 输入 1，确认打包
4.2 直接回车，使用默认docker包名



测试环境：
1. cd /data/NexOmniWeb_stage/apps/nex-omni-web
2. git pull
3. pnpm run build-only:stage
4. pnpm run docker-build:stage
4.1 输入 1，确认打包
4.2 直接回车，使用默认docker包名


百度云：
## 各个环境
cd /data/apps/NexOmniWeb-pre/apps/nex-omni-web
git pull
输入交互命令：
pnpm  build 
pnpm docker-build
4.1 输入 1，确认打包
4.2 直接回车，使用默认docker包名

官网：
cd /data/apps/NexOmniWeb-prod/apps/nex-omni-website
pnpm run build:prod
如果本地有修改被覆盖了：
/data/apps/NexOmniWeb-prod/apps/nex-omni-website/app/http/instance.ts
官网更新 替换3步骤 pnpm run build:prod