python							java						
文件命名	小写字母、下划线						驼峰命名法（CamelCase），首字母大写，后续每个单词首字母也大写						
文件扩展名	.py						.java						
变量命名	小写字母、下划线						驼峰命名法（CamelCase），首字母小写，后续每个单词首字母大写						
常量命名	大写字母、下划线						final + 大写字母、下划线						
变量类型	动态						静态						
import	1.import     2.from ... import ...   （加载模块或对象）                                        						1.类路径 完全限定名（类在哪）						
                                                    
编译、运行	逐行执行，边运行边编译						先编译后运行						
数据类型	"1.整数（int）：表示整数值，没有大小限制。
2.浮点数（float）：表示带有小数点的数值。
3.复数（complex）：包含实部和虚部的复数。
4.布尔值（bool）：表示真（True）或假（False）。
5.字符串（str）：表示文本数据。
6.列表（list）：有序、可变序列。
7.元组（tuple）：有序、不可变序列。
8.集合（set）：无序、唯一元素的集合。
9.字典（dict）：键值对的集合。"						"Java 的数据类型可以分为基本数据类型和引用数据类型。
基本数据类型包括整数类型、浮点数类型、字符类型和布尔类型。
引用数据类型包括类、接口、数组、枚举、字符串等。
Java 是一种静态类型语言，所有变量必须先声明其数据类型，类型在编译时确定。"						
                                                    
                                                    
                                                    
                                                    
                                                    
                                                    
                                                    
                                                    
代码块	4个空格缩进						{}包围						
函数	函数参数：1.位置参数     2.默认参数（定义默认参数要牢记一点：默认参数必须指向不变对象！）  3.可变参数（*args）   4.关键字参数（**dw：在函数内部组装成dict）  5.命名关键字参数（person(name, age, *, city, job):有了一个可变参数，后面跟着的命名关键字参数就不再需要一个特殊分隔符*了）   6.参数定义的顺序必须是：必选参数、默认参数、可变参数、命名关键字参数和关键字参数。                                                 						跟java的方法类似						
                                                    
                                                    
                                                    
                                                    
                                                    
                                                    
                                                    
部署	源码						编译后的文件						
